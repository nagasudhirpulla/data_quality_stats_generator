# data_quality_stats_generator

## sample run command
```
python index.py --file input/voltage_cs.xlsx --avg --max --min --sum --random
```

## TODOs
* add config.json support for edna api params like host, port
